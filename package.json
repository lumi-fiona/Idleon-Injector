{
    "name": "idleon-injector-ui",
    "version": "2.0.8",
    "description": "A cheat injector for Legends of Idleon",
    "main": "src/main.js",
    "bin": "src/main.js",
    "scripts": {
        "start": "node src/main.js",
        "build:cheats": "rollup -c",
        "watch:cheats": "rollup -c --watch",
        "validate": "npm run build:cheats && node --check cheats.js config.js && npx eslint . && node -e \"require('./src/ui/config/optionsAccountSchema.json')\"",
        "build": "npm run build:cheats && pkg . -t node18-win-x64 --compress Gzip --output InjectCheatsUI.exe",
        "build-unix": "npm run build:cheats && pkg . -t node18-linux-x64 --compress Gzip --output InjectCheatsUI-linux",
        "build-macos-x64": "npm run build:cheats && pkg . -t node18-macos-x64 --compress Gzip --output InjectCheatsUI-macos-x64",
        "build-macos-arm64": "npm run build:cheats && pkg . -t node18-macos-arm64 --compress Gzip --output InjectCheatsUI-macos-arm64"
    },
    "author": "MrJoiny",
    "dependencies": {
        "chrome-remote-interface": "^0.33.0",
        "enquirer": "^2.3.6",
        "ws": "^8.18.0"
    },
    "pkg": {
        "assets": [
            "src/ui/**/*"
        ]
    },
    "devDependencies": {
        "@eslint/js": "^9.39.2",
        "eslint": "^9.39.2",
        "globals": "^17.0.0",
        "rollup": "^4.41.0"
    }
}
